import { Button } from '@weyneshof/ui/button';
import { WButton } from '@weyneshof/ui/w-button';

export default function HomePage() {
  return (
    <div className="flex h-screen items-center justify-center space-x-2">
      <Button>aaaa</Button>
      <WButton>bbbb</WButton>
    </div>
  );
}
